# Backend API environment example

# Use DATABASE_DSN for async SQLAlchemy URL. For Postgres:
# postgresql+asyncpg://USER:PASS@HOST:PORT/DBNAME
# Example:
# DATABASE_DSN=postgresql+asyncpg://postgres:postgres@localhost:5432/bug_detector_backend
DATABASE_DSN=

# JWT signing secret (required)
JWT_SECRET=

# Backend public base URL (optional, used in links and callbacks)
# e.g., http://localhost:8000
BACKEND_BASE_URL=

# Frontend origin(s) for CORS (comma-separated for multiple)
# e.g., http://localhost:3000 or http://localhost:3000,https://app.example.com
FRONTEND_ORIGIN=http://localhost:3000

# External service URLs (for stubs/passthroughs)
ANALYSIS_ENGINE_URL=http://localhost:3001
INTEGRATION_SERVICE_URL=http://localhost:3003
