# Backend API environment example

# JWT secret used to sign tokens
JWT_SECRET=change-me

# Frontend origin(s) for CORS (comma-separated)
FRONTEND_ORIGIN=http://localhost:3000

# Database DSN (async SQLAlchemy form). Example for Postgres:
# DATABASE_DSN=postgresql+asyncpg://user:password@db:5432/bug_detector_backend
# If not set, the app will try to read from db_connection.txt. Falls back to local SQLite if none provided.
DATABASE_DSN=

# External services
ANALYSIS_ENGINE_URL=http://localhost:3001
INTEGRATION_SERVICE_URL=http://localhost:3003

# Optional: public base URL for this backend (used in links)
BACKEND_BASE_URL=http://localhost:8000
